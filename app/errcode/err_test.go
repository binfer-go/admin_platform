package errcode

import (

	"testing"
)

func TestErrCode_String(t *testing.T) {
	/*where_user_today_register := g.Map{}
	where_user_today_register["created_at between ? and ?"] = g.Slice{help.TimeStrips(0,0, -1), help.TimeStrips(0,0,0)}
	g.Dump(where_user_today_register)*/

}
